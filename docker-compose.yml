version: '3.8'

services:
  web:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./app:/app
      - ./promt_for_llm:/app/promt_for_llm
      - .:/workspace
    env_file:
      - .env
    restart: unless-stopped
